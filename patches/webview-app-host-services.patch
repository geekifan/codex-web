--- a/webview/assets/app-initial~app-main~pull-request-code-review~onboarding-page~hotkey-window-thread-page~cha~b76hmflu-y0KJWbm3.js
+++ b/webview/assets/app-initial~app-main~pull-request-code-review~onboarding-page~hotkey-window-thread-page~cha~b76hmflu-y0KJWbm3.js
@@ -48429,7 +48429,30 @@
           (super(), (this.#e = e));
         }
       }),
-      (g0e = new h0e({ appActions: Z1e, appUpdates: t0e, downloads: p0e })));
+      (g0e = new h0e({
+        appActions: Z1e,
+        appUpdates: t0e,
+        downloads: p0e,
+        projectWritableRoots:
+          window.__ELECTRON_SHIM__?.services?.projectWritableRoots,
+        threadProjectAssignments:
+          window.__ELECTRON_SHIM__?.services?.threadProjectAssignments,
+        appServerHistorySnapshots:
+          window.__ELECTRON_SHIM__?.services?.appServerHistorySnapshots,
+        localEnvironments:
+          window.__ELECTRON_SHIM__?.services?.localEnvironments,
+        pluginScheduledTasks:
+          window.__ELECTRON_SHIM__?.services?.pluginScheduledTasks,
+        threadArchive: window.__ELECTRON_SHIM__?.services?.threadArchive,
+        threadMetadataGeneration:
+          window.__ELECTRON_SHIM__?.services?.threadMetadataGeneration,
+        workspaceFiles: {},
+        requestUserInputAutoResolution: {
+          recordConversationActivity: () => undefined,
+          setConversationPresented: () => undefined,
+          snooze: () => undefined,
+        },
+      })));
   });
 function v0e() {
   let { port1: e, port2: t } = new MessageChannel();
@@ -48443,6 +48466,10 @@
   );
 }
 async function y0e() {
+  if (window.__ELECTRON_SHIM__ != null) {
+    ((by = g0e), (xy = g0e.services), xy.devboxService);
+    return;
+  }
   ((by = v0e()), (xy = await by.services), xy.devboxService);
 }
 var by,
